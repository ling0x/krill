# Krill examples

This directory contains `.agent` programs meant to exercise and document the Krill language.

## Files

- `echo.agent`: Minimal message loop and reply.
- `counter.agent`: Stateful agent with typed responses.
- `worker_pool.agent`: Fan-out / fan-in pattern with typed messages.

## Notes

Rust runtime embedding examples live under `agentr/examples/`.
